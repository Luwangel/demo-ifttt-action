name: CI

on:
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        - name: GitHub Action for IFTTT
          uses: luwangel/ifttt-action
          with:
            event: ${{secrets.IFTTT_EVENT}}
            key: ${{secrets.IFTTT_KEY}}
